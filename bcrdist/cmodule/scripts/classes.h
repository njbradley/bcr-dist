#ifndef CLASSES
#define CLASSES

#include <iostream>
#include <string>
#include <vector>
#include <fstream>
#include <map>
#include <unordered_map>
#include <sstream>

using std::cout;
using std::endl;
using std::string;
using std::vector;
using std::ifstream;
using std::istream;
using std::ofstream;
using std::ostream;
using std::unordered_map;
using std::stringstream;
using std::to_string;
using std::pair;

struct itablestream;
struct tablerow;
struct otablestream;
struct bcell_chain;
struct bcell;
struct dsbcell;
struct ssbcell;

#endif
